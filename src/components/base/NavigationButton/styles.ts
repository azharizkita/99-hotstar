import { spacing } from "@/styles/utils";
import type { CSSProperties } from "react";

export const navigationButtonStyles: CSSProperties = {
  display: "flex",
  gap: spacing(2),
  alignItems: "center",
  justifyContent: "center",
  position: "relative",
};
