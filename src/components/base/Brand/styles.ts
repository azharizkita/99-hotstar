import type { CSSProperties } from "react";

export const brandStyles: CSSProperties = {
  display: "flex",
  flexDirection: "column",
  height: "fit-content",
  flexGrow: 0,
  alignItems: "center",
  cursor: "pointer",
};
